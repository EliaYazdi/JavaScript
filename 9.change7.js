let vehicleList = ['motorbike', 'caravan', 'bike'];

function vehicleType(color, age, type) {
  let vehicle = vehicleList[type];

  if (age === 0) {
    console.log('a' + '' + color + '' + new + '' + vehicle + '.');

  } else {
    console.log('a' + '' + color + '' + old + '' + vehicle + '.');
  }

}

vehicleType('green', 0, 2);