function vehicleType(color, num) {
    let type;
    if (num === 1) {
        type = 'car';

    } else {
        type = 'motorbike';
    }
    console.log('a' + '' + color + '' + num + '.');
}



vehicleType('blue', 2);